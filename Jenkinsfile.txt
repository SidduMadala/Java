pipeline {
    agent any

    tools {
        jdk 'JDK21'
        maven 'Maven_Home'
    }

    environment {
        TEST_CLASS = 'Sahithi.Chowdary.Contacts'
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'master',
                    url: 'https://github.com/SidduMadala/Java.git',
                    credentialsId: 'github-cred-siddu'
            }
        }

        stage('Run Test') {
            steps {
                dir('Chowdary') {
                    bat "mvn clean test -Dtest=%TEST_CLASS%"
                }
            }
        }
    }
}
